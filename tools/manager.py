# -*- coding:utf-8 -*-

class manager:

    def __init__(self):
        self.local_name = '02'#判断是不是只有艾特我  使用的ID 如果换名字的话需要修改
        self.admin_key = 'key'  #设置管理员用的关键KEY
        self.admin_id = 'Ascotbe'#默认值 用来存储管理员PUID用的
